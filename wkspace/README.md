# Workspace files

This folder stores workspace descriptors for opening the repository with
preconfigured folder context.

## Usage

Open the .code-workspace file in this folder when working with the repository
in multi-root VS Code sessions.
