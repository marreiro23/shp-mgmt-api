# Common PowerShell utilities

Shared helper scripts consumed by operational PowerShell tooling.

## Purpose

- centralize project configuration loading
- avoid duplicated helper logic across scripts
- keep script behavior consistent across environments
